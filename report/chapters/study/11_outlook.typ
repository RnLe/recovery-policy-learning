= Outlook <study-outlook>

The point of naming the limits precisely is that each one implies a specific
next experiment rather than a general aspiration. Four follow.

*Budget the teacher, not the labels.* The clearest weakness of the result is
that it matches label counts while the arms differ roughly sixfold in oracle
queries (@study-results). Rerunning the same design with oracle *calls* as the
budgeted currency would answer a different and, for any setting with a human or
an expensive teacher, more relevant question. The apparatus already logs the
quantity that would become the constraint, so this is a protocol change rather
than a rebuild.

*Introduce an irreversible failure.* Every failure in this study is a timeout
(@study-failures), which removes the case where recovery supervision should
matter most. A variant with a genuinely absorbing failure state would test
whether the advantage grows, as the covariate-shift argument predicts, or
collapses because recovery labels arrive too late to help.

*Break the derangement bottleneck.* With three actions there are only two
corruption operators, so "unseen" is structurally close to "matched". A larger
action set would allow a held-out operator drawn from a genuinely different
family, and would separate two explanations that this study cannot: whether
recovery labels teach a general repair behavior or a specific inverse.

*Test the clean-slice reading directly.* If the advantage really comes from
on-policy coverage rather than perturbation repair (@study-discussion), then
labels collected in learner-visited states *without* any corruption should
capture much of it. That is a third arm, cheap to add to the existing
apparatus, and it would turn a well-supported secondary observation into a
prespecified test.

None of these requires new machinery. The contract, the ledgers, the
one-opening discipline, and the analysis path are all in place; what each
variant needs is a new frozen protocol version and its own single opening.
